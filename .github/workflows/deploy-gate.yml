name: Deploy Gatekeeper

on:
  push:
    branches: [main]

jobs:
  gatekeeper:
    # This ensures the job ONLY runs if the keyword is present
    if: contains(github.event.head_commit.message, '[deploy]')
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy keyword found. Railway will now trigger."